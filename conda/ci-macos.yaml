name: rago
channels:
  - nodefaults
  - conda-forge
dependencies:
  - python
  - pip
  - pdm >=2.19.0
  - pdm-backend
  - nodejs # used by semantic-release
  - tokenizers
  - compilers
  - uv
  - pip:
      # distlib issue
      - paginate==0.5.6
      - langdetect
      - pdm-conda
