name: rago
channels:
  - nodefaults
  - conda-forge
dependencies:
  - ollama
  - python
  - pip
  - nodejs # used by semantic-release
  - tokenizers
  - compilers
  - blis
  - uv
  - pip:
      # distlib issue
      - paginate==0.5.6
      - langdetect
      - build
      - twine
