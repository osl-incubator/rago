name: rago
channels:
  - nodefaults
  - conda-forge
dependencies:
  - ollama
  - python
  - pip
  - pdm >=2.19.0
  - pdm-backend
  - nodejs # used by semantic-release
  - tokenizers
  - compilers
  - blis
  - uv
  - pip:
      # distlib issue
      - paginate==0.5.6
      - langdetect
      - pdm-conda
