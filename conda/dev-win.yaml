name: rago
channels:
  - nodefaults
  - conda-forge
dependencies:
  - ollama
  - python 3.11.*
  - pip
  - nodejs # used by semantic-release
  - tokenizers
  - compilers
  - blis
  - uv
  - pip:
      # distlib issue
      - paginate==0.5.6
      - langdetect
      - build
      - twine
