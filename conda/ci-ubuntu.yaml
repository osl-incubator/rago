name: rago
channels:
  - nodefaults
  - conda-forge
dependencies:
  - python
  - pip
  - nodejs # used by semantic-release
  - tokenizers
  - compilers
  - uv
  - pip:
      # distlib issue
      - paginate==0.5.6
      - langdetect
      - build
      - twine
